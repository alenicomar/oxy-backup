# GitHub Actions workflow example for oxy-backup
# ================================================
# Runs a daily backup of all configured databases via cron.
#
# Prerequisites:
#   1. An oxy.yaml committed to the repo (or mounted via secrets).
#   2. Repository secrets for database credentials.
#   3. Git configured to allow pushes from Actions (deploy key or PAT).

name: Scheduled Database Backup

on:
  schedule:
    # Run daily at 02:00 UTC
    - cron: "0 2 * * *"
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  backup:
    runs-on: ubuntu-latest

    services:
      # Example: if backing up a containerized DB in CI
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: ${{ secrets.PG_PASSWORD }}
          POSTGRES_DB: app_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_health_check"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Build oxy
        run: go build -o oxy ./cmd/oxy/

      - name: Configure Git
        run: |
          git config user.name "oxy-backup[bot]"
          git config user.email "oxy-backup@users.noreply.github.com"

      - name: Run backup
        env:
          PGPASSWORD: ${{ secrets.PG_PASSWORD }}
          APP_DB_PASSWORD: ${{ secrets.APP_DB_PASSWORD }}
        run: |
          ./oxy backup \
            --config oxy.yaml \
            --log-format json \
            --log-level info

      - name: Push backup changes
        run: |
          git push origin main
